import React, {useEffect, useState} from 'react';
import LoginViewModel from "../src/page/_login/LoginViewModel";
interface IndexProps {
    isAuth: boolean;
}
const Index:React.FC<IndexProps> = ({isAuth}) => {
    return (
        <>
        </>
    )
}
export default Index;
